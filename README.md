# AI Desktop Pet

AI Desktop Pet은 사용자의 바탕화면에서 움직이며 상호작용하는 인공지능 기반의 데스크톱 펫 프로그램입니다.

---

## 🚀 현재 완료 사안
...

---

## ⚙️ 사용시 

1. (필수) 다운 받은 폴더에서 VScode/터미널/powershell 를 실행,
   `npm install` 작성 및 실행
   - 폴더에 **node_modules** 폴더가 생기면 성공


2. 실행시에는 VScode/터미널/powershell에서 
   `npm run desktop` 작성 및 실행


3. exe 파일로 만들때에는 VScode/터미널/powershell에서
   `npm run dist:installer` 작성 및 실행
   **release** 폴더에 **AI DeskPet Setup 0.0.0.exe** 실행 파일이 생기면 실행시 설치됩니다.
   (만약 실패시, 관리자 권한으로 VScode/터미널/powershell를 열어서 재시도)

---
